server:
  port: 80021

appname: dlz-framwork

spring:
  profiles:
    active: test
  application:
    name: ${appname}-test
  redis:
    #数据库索引
    database: 1
    host: 192.168.1.4
    port: 6379
    password:
    #连接超时时间
    timeout: 5000

dlz:
  api-scan-path: com.dlz
